vault:
  server:
    ingress:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: "letsencrypt-prod"
    dataStorage:
      storageClass: px-csi-db
      size: 2Gi
    auditStorage:
      enabled: true
      size: 2Gi
      storageClass: px-csi-db
    ha:
      enabled: true
      raft:
        enabled: true
