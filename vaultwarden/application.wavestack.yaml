apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vaultwarden
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'git@github.com:Sh4kE/k8s-projects.git'
    path: vaultwarden
    targetRevision: HEAD
    plugin:
      name: argocd-vault-plugin-helm
      env:
        - name: PLUGIN_PARAMS
          value: "-f values.yaml -f values.wavestack.yaml"
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: vaultwarden
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
